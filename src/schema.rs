table! {
    books (id) {
        id -> Integer,
        title -> Text,
        author -> Text,
        year -> Integer,
        publisher -> Nullable<Text>,
        description -> Nullable<Text>,
    }
}
